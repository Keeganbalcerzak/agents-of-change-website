---
import BaseLayout from "@/layouts/BaseLayout.astro";
import CTASection from "@/components/CTASection.astro";
import TestimonialsFilter from "@/components/islands/TestimonialsFilter";
import TestimonialsCarousel from "@/components/islands/TestimonialsCarousel";
import QuoteProofPairings from "@/components/QuoteProofPairings.astro";
import "@/styles/global.css";
import { getSeoMetadata, getStoryPairings, getTestimonials } from "@/lib/content";
import { findSeoByKey } from "@/lib/seo";

const [testimonials, seoEntries, pairings] = await Promise.all([
  getTestimonials(),
  getSeoMetadata(),
  Promise.resolve(getStoryPairings()),
]);
const seo = findSeoByKey(seoEntries, "success-stories");
---

<BaseLayout
  title={seo?.title ?? "Success Stories | Agents of Change"}
  description={seo?.description ?? "Read outcomes from social workers."}
  path="/success-stories"
  themeIntent="trust"
  enableMotionEngine={true}
>
  <section class="page-hero page-hero-rich" data-module="stories-hero" data-motion-tier="hero">
    <div class="container">
      <p class="eyebrow">Student outcomes</p>
      <h1 data-split-heading>Confidence transformations from social workers across the US</h1>
      <p>
        Explore narratives by exam track and state to see how candidates with similar constraints
        restructured their prep and improved outcomes.
      </p>
    </div>
  </section>

  <section
    class="section section-muted"
    data-module="story-filter"
    data-motion-tier="section"
    data-motion-variant="reveal-stagger"
    data-motion-density="compact"
    data-conversion-safe="true"
  >
    <div class="container">
      <header class="section-heading centered">
        <p class="eyebrow">Narrative library</p>
        <h2>Filter stories by track and state</h2>
        <p>Use filtered stories to find candidates with a background similar to yours.</p>
      </header>
      <TestimonialsFilter client:load testimonials={testimonials} />
    </div>
  </section>

  <QuoteProofPairings pairings={pairings} motionVariant="story-stack" />
  <section
    class="section section-muted"
    data-module="story-scroll-stack"
    data-motion-tier="section"
    data-motion-variant="story-stack"
    data-motion-density="cinematic"
  >
    <div class="container">
      <header class="section-heading centered">
        <p class="eyebrow">Depth view</p>
        <h2>Continuous proof stream</h2>
        <p>Read more passing stories from across the community.</p>
      </header>
      <TestimonialsCarousel client:load testimonials={testimonials} mode="scroll-stack" motionVariant="story-stack" />
    </div>
  </section>

  <CTASection
    title="Start your own passing story"
    description="Start your free trial and receive a weekly study plan designed around your current context."
  />
</BaseLayout>
